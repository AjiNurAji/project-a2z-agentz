# ---------------------------------------------------------------------------
# A2Z Agentz backend dependencies
# Lean, pinned where it matters. No unused/bloated packages.
# ---------------------------------------------------------------------------
fastapi
uvicorn[standard]
starlette
pydantic
pydantic_core
python-dotenv
httpx
httpcore
anyio
websockets
# Web3 / EVM tooling
web3
eth-account
eth-utils
eth_abi
rlp
# LLM client (Agent A/B inference via OpenAI-compatible vLLM endpoint)
openai
# Database (psycopg2 is the driver used by database.py; asyncpg is NOT used)
psycopg2-binary
# Scheduler (agent_runner BackgroundScheduler)
apscheduler
# Async HTTP client used by Agent A/B cycles
aiohttp
# Password hashing (auth.py)
bcrypt
# Structured JSON logging
python-json-logger
# JWT validation for WS + HTTP auth (auth.py)
PyJWT>=2.8.0
# Semantic dedup (agent_a_chroma.py lazy-imports chromadb at runtime)
chromadb
